import javax.swing.text.*;
import javax.swing.*;
import java.awt.*;
import java.awt.event.*;
import java.util.*;
    
class Demo{
    public static void main(String args[]){
        
        //JLayeredPane = Swing container that provides a third dimension for positioning components
        JLabel label1 = new JLabel();
        label1.setOpaque(true);
        label1.setBackground(Color.RED);
        label1.setBounds(50,50,200,200); //this is for setting the bounds of the label
        
        JLabel label2 = new JLabel();
        label2.setOpaque(true);
        label2.setBackground(Color.GREEN);
        label2.setBounds(100,100,200,200);
        
        JLabel label3 = new JLabel();
        label3.setOpaque(true);
        label3.setBackground(Color.BLUE);
        label3.setBounds(150,150,200,200);
        
        JLayeredPane layeredPane = new JLayeredPane(); //this is for creating a layered pane
        layeredPane.setBounds(0,0,500,500);
        
        JFrame frame = new JFrame("JLayeredPane");
        frame.add(layeredPane);
        layeredPane.add(label1 , JLayeredPane.DEFAULT_LAYER); //this is for adding the label to the layered pane and setting the layer of the label
        layeredPane.add(label2 , JLayeredPane.DEFAULT_LAYER);
        layeredPane.add(label3 , JLayeredPane.DRAG_LAYER); //this is for adding the label to the layered pane and setting the layer of the label
        
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.setSize(500,500);
        frame.setLayout(null);
        
        frame.setVisible(true);
        }
    }
